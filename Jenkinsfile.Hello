@Library('pipeline-library-demo')_

// To use library with a specific version or branch, use the following syntax:
//@Library('pipeline-library-demo@master')_ or @Library('[library name]@[library version]')_

stage('Echo Hello') {
    sayHello 'Jenkins'
}